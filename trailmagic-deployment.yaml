apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: trailmagic
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: trailmagic
        name: trailmagic
    spec:
      containers:
      - image: us.gcr.io/horsecodes/trailmagic-web:8adb04d757d02dd11ad8ac368129426035171197
        name: trailmagic
        resources:
          requests:
            cpu: 0.25
          limits:
            memory: 512Mi
        ports:
        - containerPort: 80
          protocol: TCP
        # livenessProbe:
        #   httpGet:
        #     path: /
        #     port: 80
        #   initialDelaySeconds: 30
        #   timeoutSeconds: 45
