apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: trailmagic
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: trailmagic
        name: trailmagic
    spec:
      containers:
      - image: us.gcr.io/horsecodes/trailmagic-web:b485ba5a12a1ae5f03dd9a35ac3384ace35454b8
        name: trailmagic
        resources:
          requests:
            cpu: 0.25
          limits:
            memory: 512Mi
        ports:
        - containerPort: 80
          protocol: TCP
        # livenessProbe:
        #   httpGet:
        #     path: /
        #     port: 80
        #   initialDelaySeconds: 30
        #   timeoutSeconds: 45
